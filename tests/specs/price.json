[
  {
    "name": "empty watchlist shows message",
    "steps": [
      {
        "send": { "callback": "price:show" },
        "expect": [
          {
            "method": "editMessageText",
            "payload": { "text": "Your watchlist is empty — tap 🪙 Watchlist to add some coins." }
          }
        ]
      }
    ]
  }
]
