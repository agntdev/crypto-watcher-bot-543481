[
  {
    "name": "empty watchlist shows message for alerts",
    "steps": [
      {
        "send": { "callback": "alerts:manage" },
        "expect": [
          {
            "method": "editMessageText",
            "payload": { "text": "Your watchlist is empty — add coins first, then set up alerts." }
          }
        ]
      }
    ]
  }
]
